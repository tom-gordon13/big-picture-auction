{
  "buildCommand": "cd ui && npm install && npm run build",
  "outputDirectory": "ui/build",
  "installCommand": "cd backend && npm install --include=dev && npx prisma generate && npm run build",
  "functions": {
    "api/index.js": {
      "runtime": "nodejs20.x",
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
